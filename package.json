{
  "name": "chartora-in",
  "version": "3.0.0",
  "description": "CHARTORA — Trading Intelligence, Market Alerts & Trading Education Platform",
  "private": true,
  "scripts": {
    "dev": "python3 -m http.server 8080",
    "build": "mkdir -p dist && cp -r index.html styles js public legal README.md DEPLOYMENT.md dist/ && cp public/_redirects public/_headers dist/",
    "predeploy": "npm run build",
    "deploy": "npm run build && wrangler pages deploy dist",
    "preview": "python3 -m http.server 8080 --directory dist"
  },
  "devDependencies": {
    "wrangler": "^3.114.0"
  },
  "keywords": [
    "trading",
    "market-intelligence",
    "forex",
    "gold",
    "technical-analysis",
    "tradingview",
    "mt5"
  ],
  "author": "CHARTORA",
  "license": "MIT"
}
